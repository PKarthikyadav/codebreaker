⭐ Q4 — Create EC2 + Deploy App Using Docker (Beginner-Friendly Guide)

We will deploy your GitHub application (your simple webapp) on an Ubuntu EC2 server using Docker.

✅ PART 1 — Launch an EC2 Instance
1. Login to AWS Console

https://console.aws.amazon.com

2. Open EC2 Dashboard
3. Click Launch Instance
4. Fill details:

Name → exam-ubuntu-app

AMI → Ubuntu Server 22.04 LTS

Instance type → t2.micro (free tier)

Key pair → Create new (download .pem)

Network settings:

Allow HTTP (port 80)

Allow SSH (port 22)

5. Click Launch Instance
6. When ready → copy the Public IP

You will need it later.

✅ PART 2 — Connect to EC2 (SSH)

On your laptop:

ssh -i "yourkey.pem" ubuntu@YOUR_PUBLIC_IP


Example:

ssh -i "exam.pem" ubuntu@13.201.24.70

✅ PART 3 — Install Docker + Git

Run these commands AFTER you connect:

sudo apt update
sudo apt install -y docker.io git
sudo systemctl enable docker
sudo systemctl start docker


Check docker:

docker --version

✅ PART 4 — Clone your GitHub application

Example (your repo):

git clone https://github.com/PKarthikyadav/webapp.git
cd webapp

✅ PART 5 — Create a Dockerfile

Inside EC2, run:

nano Dockerfile


Paste this:

FROM tomcat:9.0

COPY target/*.war /usr/local/tomcat/webapps/myapp.war


Save:
Press CTRL + O, then ENTER
Exit:
Press CTRL + X

❗ But first you must build the WAR file (like earlier)

Run:

sudo apt install -y maven
mvn clean package


WAR will be inside:

target/*.war

✅ PART 6 — Build Docker Image

Run:

docker build -t myapp .

✅ PART 7 — Run the container

Run Tomcat container serving your WAR:

docker run -d -p 80:8080 --name myapp-container myapp


Now your app runs on port 80, so it's public.

✅ PART 8 — Test Deployment

Open browser on your laptop:

👉 http://YOUR_PUBLIC_IP/myapp/

You should see your deployed page:

Hello from Jenkins Deployment!
Time: ...


Take a screenshot.

🎯 Exam Output: What You Must Show
1. EC2 instance details

From AWS:

Instance ID

Public IP

Status: running

Ubuntu AMI

2. Terminal commands screenshot

Show:

docker install

git clone

mvn package

docker build

docker run

3. Browser Deployment Proof

Open:

http://PUBLIC_IP/myapp


Show the webpage.
This is your final deployment proof.
